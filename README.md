# Minimal Music

As you can see, this is a minimalist music playback software.

## Run

Ensure that you have installed the node.js, and then run the following command.

```bash
npm install
npm run start
```

## Build

Run the following command to build the installer exe.

```bash
npm run build
```
